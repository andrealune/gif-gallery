node_modules/
dist/
.env
*.log
coverage/
# Local-development-only storage driver output (STORAGE_LOCAL_DIR default);
# anchored to the server/ root so it doesn't also match src/storage (the
# storage abstraction's source code).
/storage/
