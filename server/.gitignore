node_modules/
dist/
.env
*.log
coverage/
# Local-development-only storage driver output (STORAGE_LOCAL_DIR default,
# consumed by src/storage's "local" provider and the older
# src/services/storage helper); anchored to the server/ root so it doesn't
# also match source directories named "storage" (src/storage, src/services/storage, test/storage).
/storage/
