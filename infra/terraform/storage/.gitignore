# .terraform.lock.hcl is intentionally NOT ignored: it pins exact provider
# versions/hashes for reproducible applies and should be committed.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
*.auto.tfvars
crash.log
override.tf
override.tf.json
